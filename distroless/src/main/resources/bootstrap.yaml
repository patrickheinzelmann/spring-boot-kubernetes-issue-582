spring:
  application:
    name: example-distroless
  cloud:
    kubernetes:
      reload:
        enabled: true
        mode: polling
        period: 15000
      config:
        sources:
          - name: example-distroless